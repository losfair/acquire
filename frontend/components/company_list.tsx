import { companyColorClasses } from "@/utils/colors";
import { decodeSheet, encodeSheetRC } from "@/utils/sheet_util";
import { Card, Company, RoomSummary } from "@/utils/types";
import { useMemo } from "react";

export default function CompanyList({ summary }: { summary: RoomSummary }) {

    return (
        <div>
        </div>
    )
}